# =============================================================================
# Nginx reverse proxy for Orthanc REST API
# Manages TLS termination and WebSocket upgrade for the Orthanc Explorer UI.
# =============================================================================

# Redirect HTTP → HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name pacs.example.com;   # Replace with your FQDN

    # TLS — generated by certbot or supply your own certs
    ssl_certificate     /etc/letsencrypt/live/pacs.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pacs.example.com/privkey.pem;

    # Modern TLS settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000" always;
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;

    # Increase timeouts / body size for large DICOM uploads
    client_max_body_size 4G;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;

    # Orthanc REST API + Explorer
    location / {
        proxy_pass http://127.0.0.1:8042;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (Orthanc Explorer 2)
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
