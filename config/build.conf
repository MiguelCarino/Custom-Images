# config/build.conf — global build defaults (plain bash, sourced).
# Override at invocation time with env vars or build.sh --registry/--tag flags.

FEDORA_VERSION=44
REGISTRY=localhost
TAG=latest
BASE_IMAGE="quay.io/fedora/fedora-bootc:${FEDORA_VERSION}"
