# Generated by lib/generate.sh — DO NOT EDIT (regen: ./build.sh generate carino-llms)
# Layer: purpose:llms
# Chain: base desktop-common de/cosmic-hyprland purpose:llms
# Build: podman build -f generated/carino-llms/Containerfile generated/carino-llms/context

FROM localhost/carino-layer-de-cosmic-hyprland:latest

LABEL org.opencontainers.image.title="carino-llms" \
      org.opencontainers.image.description="Local LLMs: ollama, llama.cpp, whisper.cpp, Python tooling" \
      org.opencontainers.image.base.name="quay.io/fedora/fedora-bootc:44"

# Packages
RUN dnf -y install ollama llama-cpp whisper-cpp ramalama python3-huggingface-hub uv pipx git-lfs numactl nvtop radeontop && dnf clean all

# Services (enable)
RUN systemctl enable ollama.service

# Flatpak list (installed on first boot by flatpak-setup.service)
RUN mkdir -p /usr/share/fedora-images/flatpaks.d && \
    printf '%s\n' com.jeffser.Alpaca io.podman_desktop.PodmanDesktop > /usr/share/fedora-images/flatpaks.d/40-llms.list

# Lint the resulting bootc image
RUN bootc container lint
