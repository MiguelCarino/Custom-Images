# Generated by lib/generate.sh — DO NOT EDIT (regen: ./build.sh generate carino-music)
# Layer: purpose:music
# Chain: base desktop-common de/cosmic-hyprland purpose:music
# Build: podman build -f generated/carino-music/Containerfile generated/carino-music/context

FROM localhost/carino-layer-de-cosmic-hyprland:latest

LABEL org.opencontainers.image.title="carino-music" \
      org.opencontainers.image.description="Music production: DAWs, plugin suites, low-latency audio tuning" \
      org.opencontainers.image.base.name="quay.io/fedora/fedora-bootc:44"

# Packages
RUN dnf -y install ardour9 qtractor hydrogen Carla qpwgraph lsp-plugins-lv2 lv2-x42-plugins fluidsynth fluid-soundfont-gm realtime-setup rtkit tuned rt-tests && dnf clean all

# Services (enable)
RUN systemctl enable tuned.service

# Services (mask)
RUN systemctl mask power-profiles-daemon.service

# Kernel arguments (slot 40)
RUN mkdir -p /usr/lib/bootc/kargs.d && \
    echo 'kargs = ["threadirqs"]' > /usr/lib/bootc/kargs.d/40-music.toml

# Lint the resulting bootc image
RUN bootc container lint
